apiVersion: apps/v1
kind: Deployment
  labels:
    app: inkpot-web-deployment
  name: inkpot-web-deployment
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: inkpot-web-deployment
    spec:
      containers:
      - image: gcr.io/PROJECT_ID/IMAGE:TAG
        ports:
        - containerPort: 80
          protocol: TCP